#!/usr/bin/python3
"""Exact Same Module"""


def is_same_class(obj, a_class):
    """
    Checks if an instance of the same class
    """
    if type(obj) is a_class:
        return True
    return False
